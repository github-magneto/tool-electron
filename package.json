{
  "name": "app",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "electron": "^21.3.1",
    "electron-packager": "^17.1.1"
  },
  "scripts": {
    "start": "electron src/",
    "build": "electron-packager ./ electron --all --out=./dist --app-version=1.0.0 --overwrite"
  }
}
